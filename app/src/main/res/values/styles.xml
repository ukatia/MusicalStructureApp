<resources>

    <!-- Base application theme. -->
   <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
           <!-- Customize your theme here. -->
      <item name="colorPrimary">@color/colorPrimary</item>
      <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
      <item name="colorAccent">@color/colorAccent</item>
  </style>

    <style name="Title">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/marginTitle</item>
        <item name="android:layout_marginLeft">@dimen/marginTitle</item>
        <item name="android:textSize">@dimen/sizeTitle</item>
        <item name="android:textColor">@color/colorTitle</item>
    </style>

    <style name="btn">
        <item name="android:layout_width">@dimen/widthBtn</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">@dimen/drawablePaddingBtn</item>
        <item name="android:drawablePadding">@dimen/drawablePaddingBtn</item>
        <item name="android:padding">@dimen/paddingBtn</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/colorBtnText</item>
    </style>

    <style name="img">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginTop">@dimen/marginTopImg</item>
    </style>

</resources>
